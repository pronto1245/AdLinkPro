{
  "name": "affilix",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "tsx server/index.ts"
  },
  "dependencies": {},
  "devDependencies": {
    "tsx": "^4.7.0"
  }
}
