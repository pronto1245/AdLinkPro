Цель:
Проверить, правильно ли настроена архитектура сайта и корректно ли связаны между собой все ключевые страницы и модули. Убедиться, что вся информация отображается и приходит туда, куда нужно, в нужных форматах, в реальном времени, и соответствует логике системы.

🧩 Объекты для проверки:
Следующие разделы должны быть протестированы на целостность логики, связи и корректность данных:

Дашборд

Пользователи

Аналитика пользователей

Роли и права доступа

Офферы

Финансы

Антифрод-система

Постбеки

Аналитика / Статистика

Система тикетов и поддержки

✅ Что требуется протестировать:
🔗 Связность:
Убедиться, что все указанные модули связаны между собой, и данные между ними передаются корректно.

Проверить, что действия на одной странице (например, регистрация, клик, добавление оффера, назначение роли) отражаются в других модулях (например, аналитике, постбеках, финансах, антифроде).

📊 Корректность аналитики:
Все события (клики, уники, регистрации, депозиты, лиды, продажи) должны отображаться корректно в разделе "Аналитика / Статистика".

Постбеки должны срабатывать и передавать всю необходимую информацию в реальном времени.

🔄 Входящие данные:
Все внешние события (API, постбеки, редиректы, действия пользователей) должны попадать в нужные таблицы, в нужной логике, с правильной связкой ID:

Click ID

Partner ID

Offer ID

Campaign ID

SubID 1–30

🔐 Роли и права:
Проверить, что разные роли (владелец, рекламодатель, партнёр) видят только свою часть, и у них нет доступа к данным других ролей, кроме Super Admin.

🧾 Финансы:
Убедиться, что в разделе финансов корректно:

Отображаются доходы, выплаты, расходы.

Считается комиссия платформы.

Отражаются постбэки, начисления, холды и выплаты по ролям.

⚠️ Антифрод:
Проверить, что фрод-индикаторы отображаются, логируются, и соответствуют реальной активности (бот, VPN, прокси, дубль, подозрительная частота кликов и т.д.).

💬 Поддержка / Тикеты:
Убедиться, что система тикетов работает:

Партнёр может отправить тикет.

Рекламодатель и админ могут ответить.

Статусы (открыт / решён / в работе) меняются корректно.

Уведомления приходят соответствующим ролям.

❓ Вопросы к тесту:
Все ли события корректно отражаются во всех модулях, и совпадают ли цифры между дашбордом, аналитикой и детализацией?

Откуда и на каких условиях каждый модуль получает данные?

Нет ли потерь Click ID, SubID, Offer ID, Partner ID в процессе?

Все ли страницы и действия привязаны к системе аналитики, BI и постбеков?

Срабатывает ли антифрод и отображается ли его статистика?

Работает ли поддержка (тикеты): создаются, приходят в нужную роль, можно вести переписку?

Все ли уведомления и алерты приходят по назначению (email, кабинет, Telegram)?

Все ли действия логируются (для аудита и последующей диагностики)?

🛠 Требуемый результат:
Подтверждение, что архитектура настроена верно.

Все данные корректно поступают и отражаются в нужных модулях.

Все роли видят только своё, а владелец — всё.

Ошибки и недочёты документируются и передаются на исправление.