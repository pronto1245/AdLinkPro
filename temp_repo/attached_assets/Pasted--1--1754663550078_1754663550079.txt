 Общая логика кастомного домена для офферов
🧩 1. Добавление кастомного домена
Рекламодатель в личном кабинете открывает раздел «Кастомные домены» и вводит желаемый домен, например:

Copy
Edit
trk.mybrand.com
🧾 2. Генерация уникального кода подтверждения
Система автоматически создаёт уникальный идентификатор (например, verify=abc1234567) — он нужен для подтверждения владения доменом.

📡 Варианты подтверждения
🔸 A. DNS-подтверждение
Пользователь получает инструкцию:

Тип: TXT

Хост: trk.mybrand.com

Значение: verify=abc1234567

Он добавляет эту запись в DNS-зону своего домена у провайдера (REG.RU, Cloudflare и т. д.).

Система периодически или по кнопке «Проверить» делает DNS-запрос, ищет запись и сверяет значение.

🔸 B. Файл на сервере (альтернатива)
Если домен поддерживает размещение файлов, можно загрузить .txt:

URL: https://trk.mybrand.com/trk-verification.txt

Содержимое: verify=abc1234567

✅ 3. Подтверждение владения
Если DNS-запись найдена (или файл доступен) и значение верное:

Домен получает статус «✅ Подтверждён»

Можно использовать для генерации ссылок

🔐 4. Проксирование и SSL
Сервер платформы проксирует все запросы через кастомный домен:

Прокси NGINX / Cloudflare / Vercel / Netlify

Автоматически подключается SSL (Let's Encrypt)

🔗 5. Генерация трек-ссылок
Когда рекламодатель создаёт оффер:

Если выбран кастомный домен, система оборачивает ссылку:

Пример:

arduino
Copy
Edit
https://trk.mybrand.com/click?offer=123&clickid=partnerABC
👤 6. Поведение у партнёра
Партнёр получает трек-ссылку с кастомным доменом, например:

arduino
Copy
Edit
https://trk.mybrand.com/click?offer=123&clickid=partnerXYZ
Внутри ссылка уже включает clickid=partnerID — это подставляется системой автоматически при генерации.

Партнёр может добавить другие саб-айди вручную (например, sub1=googleads).

📂 7. Условия доступа
Кастомный домен используется только у подтверждённых рекламодателей

У каждого оффера можно выбирать, какой домен использовать (платформенный или кастомный)

🧾 8. Отображение и логика в системе
У подтверждённых доменов статус «🟢 Подключен»

У неподтверждённых — «⚠️ Не подтверждён» с подсказкой

Партнёр в кабинете:

Видит кастомный домен в трек-ссылке

Использует именно его при продвижении оффера